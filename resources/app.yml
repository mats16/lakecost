resources:
  apps:
    finlake:
      name: finlake
      description: Databricks Apps FinOps dashboard (DBU + cloud cost visibility)
      source_code_path: ../
      resources:
        - name: warehouse
          sql_warehouse:
            id: ${var.sql_warehouse_id}
            permission: CAN_USE
        # Uncomment to bind Lakebase. Leave commented when Lakebase isn't available.
        # - name: lakebase
        #   database:
        #     instance_name: ${var.lakebase_instance_name}
        #     permission: CAN_CONNECT_AND_CREATE_DBS
